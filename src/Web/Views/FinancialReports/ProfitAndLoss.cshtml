@{
    ViewData["Title"] = "Profit & Loss";
}

<div class="d-flex flex-wrap justify-content-between align-items-start gap-2 mb-3 no-print">
    <div>
        <h4 class="text-primary mb-1">Profit &amp; Loss</h4>
        <p class="text-muted small mb-0">Income statement for a date range</p>
    </div>
    <div class="d-flex gap-2">
        <button type="button" class="btn btn-outline-primary btn-sm" id="btn-print-report">
            <i class="fa-solid fa-print me-1"></i>Print / Save PDF
        </button>
        <a asp-action="Index" class="btn btn-outline-secondary btn-sm">
            <i class="fa-solid fa-arrow-left me-1"></i>Back
        </a>
    </div>
</div>

<div id="report-company-warning" class="alert alert-warning d-none mb-3 no-print"></div>

<div class="card p-3 mb-3 no-print">
    <div class="row g-3 align-items-end">
        <div class="col-md-3">
            <label class="form-label" for="filter-from">From Date</label>
            <input type="date" class="form-control" id="filter-from" />
        </div>
        <div class="col-md-3">
            <label class="form-label" for="filter-to">To Date</label>
            <input type="date" class="form-control" id="filter-to" />
        </div>
        <div class="col-md-3">
            <button type="button" class="btn btn-primary w-100" id="btn-load-report">
                <i class="fa-solid fa-magnifying-glass me-1"></i>Load
            </button>
        </div>
    </div>
</div>

<div class="card p-4 report-print-area" id="report-content">
    <div class="text-center mb-4">
        <h5 class="text-primary mb-1">Profit &amp; Loss Statement</h5>
        <p class="text-muted small mb-0" id="report-period">Select dates and click Load</p>
    </div>

    <div class="table-responsive">
        <table class="table table-sm table-bordered mb-0">
            <thead class="table-light">
                <tr>
                    <th>Account #</th>
                    <th>Account Name</th>
                    <th>Section</th>
                    <th class="text-end">Amount</th>
                </tr>
            </thead>
            <tbody id="report-lines">
                <tr><td colspan="4" class="text-muted text-center">No data loaded.</td></tr>
            </tbody>
            <tfoot id="report-footer" class="d-none table-light">
                <tr>
                    <th colspan="3" class="text-end">Total Revenue</th>
                    <th class="text-end" id="report-total-revenue">0.00</th>
                </tr>
                <tr>
                    <th colspan="3" class="text-end">Total COGS</th>
                    <th class="text-end" id="report-total-cogs">0.00</th>
                </tr>
                <tr>
                    <th colspan="3" class="text-end">Gross Profit</th>
                    <th class="text-end" id="report-gross-profit">0.00</th>
                </tr>
                <tr>
                    <th colspan="3" class="text-end">Total Expenses</th>
                    <th class="text-end" id="report-total-expenses">0.00</th>
                </tr>
                <tr class="fw-bold">
                    <th colspan="3" class="text-end">Net Profit</th>
                    <th class="text-end" id="report-net-profit">0.00</th>
                </tr>
            </tfoot>
        </table>
    </div>
</div>

@section Styles {
    <style>
        @@media print {
            .sidebar, .top-navbar, .content-header, .app-footer, .no-print { display: none !important; }
            .main-wrapper { margin: 0 !important; }
            .content-area { padding: 0 !important; }
            .report-print-area { border: none !important; box-shadow: none !important; }
        }
    </style>
}

@section Scripts {
    <script src="~/js/modules/financial-report-profit-loss.js" asp-append-version="true"></script>
}
