@{
    ViewData["Title"] = "Related Company Purchase";
}

<div class="d-flex flex-wrap justify-content-between align-items-start gap-2 mb-3 no-print">
    <div>
        <h4 class="text-primary mb-1">Related Company Purchase</h4>
        <p class="text-muted small mb-0">Al Baasit, Arian Traders, Kashaf Polyester, and Al Wahhab Merchants</p>
    </div>
    <div class="d-flex gap-2">
        <button type="button" class="btn btn-outline-primary btn-sm" id="btn-print-report">
            <i class="fa-solid fa-print me-1"></i>Print / Save PDF
        </button>
        <a asp-action="Index" class="btn btn-outline-secondary btn-sm">
            <i class="fa-solid fa-arrow-left me-1"></i>Back
        </a>
    </div>
</div>

<div id="report-company-warning" class="alert alert-warning d-none mb-3 no-print"></div>

<div class="card p-3 mb-3 no-print">
    <div class="row g-3 align-items-end">
        <div class="col-md-2">
            <label class="form-label" for="filter-from">From Date</label>
            <input type="date" class="form-control" id="filter-from" />
        </div>
        <div class="col-md-2">
            <label class="form-label" for="filter-to">To Date</label>
            <input type="date" class="form-control" id="filter-to" />
        </div>
        <div class="col-md-3">
            <label class="form-label" for="filter-vendor">Vendor</label>
            <select class="form-select select2" id="filter-vendor">
                <option value="">All related companies</option>
            </select>
        </div>
        <div class="col-md-2">
            <div class="form-check mt-4">
                <input class="form-check-input" type="checkbox" id="filter-approved-only" checked />
                <label class="form-check-label" for="filter-approved-only">Approved only</label>
            </div>
        </div>
        <div class="col-md-2">
            <button type="button" class="btn btn-primary w-100" id="btn-load-report">
                <i class="fa-solid fa-magnifying-glass me-1"></i>Load
            </button>
        </div>
    </div>
</div>

<div class="card p-3 report-print-area report-print-fit" id="report-content" data-related-companies-only="true">
    <div class="text-center mb-4">
        <h5 class="text-primary mb-1">Related Company Purchase</h5>
        <p class="text-muted small mb-0" id="report-period">Select dates and click Load</p>
    </div>

    <div class="table-responsive">
        <table class="table table-sm table-bordered mb-0">
            <thead class="table-light">
                <tr>
                    <th>Vendor Name</th>
                    <th class="text-end">Bills</th>
                    <th class="text-end">Ex-value</th>
                    <th class="text-end">Tax</th>
                    <th class="text-end">Net Amount</th>
                </tr>
            </thead>
            <tbody id="report-lines">
                <tr><td colspan="5" class="text-muted text-center">No data loaded.</td></tr>
            </tbody>
            <tfoot id="report-footer" class="d-none table-light">
                <tr>
                    <th class="text-end">Totals</th>
                    <th class="text-end" id="report-total-bills">0</th>
                    <th class="text-end" id="report-total-exvalue">0.00</th>
                    <th class="text-end" id="report-total-tax">0.00</th>
                    <th class="text-end" id="report-total-net">0.00</th>
                </tr>
            </tfoot>
        </table>
    </div>
</div>

@section Styles {
    <link rel="stylesheet" href="~/css/report-print.css" asp-append-version="true" />
}

@section Scripts {
    <script src="~/js/modules/report-print-common.js" asp-append-version="true"></script>
    <script src="~/js/modules/purchase-report-by-vendor.js" asp-append-version="true"></script>
}
